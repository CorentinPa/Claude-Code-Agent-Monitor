---
description: Read-only CCAM platform status across hooks, config, updates, and MCP prerequisites
---

Run `ccam health`, `ccam hooks status`, `ccam config claude overview`,
`ccam config codex overview`, and `ccam update-check`. Report exact provider
hook state, configuration roots, dashboard version, and any missing MCP build.
Do not modify files or install hooks.
