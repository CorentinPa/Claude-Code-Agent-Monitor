interface:
  display_name: "Version Release"
  short_description: "Classify and apply synchronized CCAM semantic version bumps."
  default_prompt: "Use $version-release to choose the correct patch, minor, or major bump and synchronize every CCAM release surface."
policy:
  allow_implicit_invocation: false
